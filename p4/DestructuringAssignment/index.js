/* array destructuring assignment */
const arr = [1, 2, 3, 4, 5];

const [one, two, three] = arr;

/* object destructuring assignment */
const obj = {
    x: 10,
    y: 20
};

const { x, y } = obj;
console.log(x, y);