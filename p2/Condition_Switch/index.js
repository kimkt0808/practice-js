const num = 10;

/* break를 만날 때까지 계속 실행. */
switch (num) {
    case 1:
        console.log(num);
        break;
    case 2:
        console.log(num);
        break;
    default:
        console.log("아무것도 해당되지 않음.");
}